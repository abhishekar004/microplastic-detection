# Git Setup Commands
# Run these commands one by one in PowerShell

# Step 1: Configure Git (REQUIRED - Replace with your info)
# git config --global user.name "Your Name"
# git config --global user.email "your.email@example.com"

# Step 2: Create initial commit
# git commit -m "Initial commit: Microplastic Detection App"

# Step 3: After creating GitHub repo, add remote and push
# git remote add origin https://github.com/YOUR_USERNAME/microplastic-app.git
# git branch -M main
# git push -u origin main

